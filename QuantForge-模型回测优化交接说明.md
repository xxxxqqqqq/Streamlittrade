# QuantForge 模型回测优化交接说明

更新时间：2026-08-17（Asia/Shanghai）  
本地项目：`C:\Users\we\Streamlittrade`  
生产服务器：`8.134.107.95`  
服务器项目目录：`/opt/quantforge`  
当前分支：`main`  
当前提交：`1d3a801`

## 一、接手目标

继续维护现有 QuantForge 项目，不重新搭建项目，不未经确认修改或清理生产研究数据。

本阶段的核心目标是优化标准行情、模型研究和组合回测之间的时间边界，避免出现以下问题：

1. 将调参区表现误认为最终样本外表现。
2. 从样本外预测中手工截取表现较好的日期进行回测。
3. 看到最终封存区结果后再修改选股阈值、持仓数量或交易成本参数。
4. 最终封存区回测错误读取调参区预测文件。
5. 没有股票达到入选阈值时，将科学上有效的“全现金结果”错误判定为任务失败。

期望形成的标准流程为：

`标准行情版本 → 不可变因子快照 → 研究数据集 → 训练区 → 调参区 OOS → 锁定模型和组合规则 → 最终封存区 OOS → 组合回测`

## 二、已完成内容

### 1. 模型回测作用域拆分

模型组合回测现已明确分为：

- `tuning_oos`：调参区样本外回测，用于比较模型和组合规则，不能称为最终表现。
- `sealed_oos`：最终封存区回测，用于锁定模型后的最后一次检验。

为了兼容已有接口，`signal_source` 仍保持为 `model_oos`，新增 `prediction_scope` 区分上述两种作用域。

### 2. 日期范围硬约束

模型回测不再接受用户自行选择一段日期：

- 调参区回测强制使用模型研究记录中的完整 tuning 起止日期。
- 最终封存区回测强制使用封存评估记录中的完整 sealed 起止日期。
- API 会覆盖客户端传入的开始和结束日期。
- Worker 会再次校验任务载荷，防止绕过 API 或篡改任务参数。

该约束用于防止挑选表现较好的月份或年份。

### 3. 最终封存区组合规则预登记

开启最终封存区前，管理员必须同时登记以下组合和成交规则：

- Top-N 持仓数量
- 最低入选概率
- 调仓频率
- 初始资金
- 最大成交量参与率
- A 股整手股数
- 佣金率
- 最低佣金
- 印花税率
- 滑点率

这些参数保存在封存评估任务和指标快照中。创建最终封存区回测时，服务端会强制覆盖为预登记参数，页面也会禁用修改。

### 4. 最终封存预测产物接入组合回测

原逻辑始终读取 `ModelVersion.prediction_artifact_uri`，该文件实际上只包含调参区 OOS 预测。

现逻辑为：

- `tuning_oos` 读取模型的调参区预测产物。
- `sealed_oos` 读取 `SealedEvaluation.artifact_uri`。
- 回测审计记录预测作用域、预测日期、预测哈希、模型拟合截止日、数据版本和组合规则来源。

### 5. 历史封存记录兼容

旧封存评估是在“组合规则预登记”功能上线前生成的，因此没有保存预登记参数。

兼容规则为：

- 使用固定系统默认组合规则执行。
- 明确标记为 `legacy_default_not_preregistered`。
- 页面和报告不得将其描述为“封存前已登记”。

默认规则为：Top-N 5、最低概率 0.55、每 5 个交易日调仓、初始资金 100 万元，以及现有保守成交成本设置。

### 6. 零入选组合处理

只读验证发现，现有 user2 历史封存模型在 0.55 最低概率下，没有股票满足入选条件。

原逻辑会抛出错误。现已改为：

- 正常生成全现金净值序列。
- 成交笔数为 0。
- 保留完整预测覆盖和调仓审计记录。
- 写入 `has_eligible_selections=false` 和 `selection_warning`。
- 不会为了产生交易而偷偷降低概率阈值。

### 7. 批量预测用途标记

批量预测文件新增用途标记：

- `live_forward`：所有评分日期均晚于模型拟合截止日。
- `historical_rescore_not_backtest_eligible`：包含模型已经见过的历史日期。

批量预测摘要统一记录 `historical_backtest_eligible=false`，避免将普通历史重算冒充样本外回测。

### 8. 页面说明完善

已更新：

- 模型回测创建页：可选择调参区或最终封存区，并显示对应限制。
- 模型详情页：开启封存区前登记组合规则，并分别提供调参区和封存区回测入口。
- 回测详情页：明确显示 `TUNING OOS` 或 `FINAL SEALED OOS`，并展示日期政策和规则来源。
- 回测记录页：区分调参区模型信号和最终封存区模型信号。

## 三、关键决定

### 1. 标准行情可以覆盖全部研究年份

标准行情保存 2017—2025 年数据本身没有问题。需要隔离的是模型研究用途，而不是物理拆成互不相干的行情文件。

当前三段式数据边界示例：

- 训练区：2018-07-02 至 2022-07-28
- 调参区：2022-09-26 至 2024-06-11
- 最终封存区：2024-08-07 至 2025-12-03
- Purge：20 个交易日
- Embargo：20 个交易日

### 2. 调参区可以反复比较，但不能称为最终结果

允许在完整调参区比较模型和组合参数；最终封存区只用于锁定后的最后检验。

### 3. 最终封存区不能根据结果再次调参

最终封存结果即使不好，也不能降低阈值或更换参数后重复声称是“最终样本外结果”。需要继续研究时，应创建新的研究数据集和新一代实验，并使用后续真正未见数据进行验证。

### 4. 零交易也是有效结果

预登记阈值过高导致没有入选股票，是有效的研究结论。系统应生成全现金报告，而不是报错或暗中放宽标准。

### 5. 不迁移历史生产数据

本次没有增加数据库字段，也没有修改已有生产研究记录。预登记规则保存在现有 JSON 任务载荷和指标字段中，旧记录通过显式兼容标记处理。

## 四、提交记录

### `b39df11` — Enforce honest model backtest periods

实现预测作用域、日期硬约束、封存规则预登记、封存预测回测、批量预测用途标记及页面说明。

### `1d3a801` — Keep zero-selection model backtests auditable

允许没有股票达到预登记阈值时生成可审计的全现金回测报告。

两个提交均已推送至远程 `origin/main` 并部署。

## 五、修改过的文件

### 后端接口与服务

- `backend/app/api/backtests.py`
- `backend/app/api/research.py`
- `backend/app/schemas/backtest.py`
- `backend/app/schemas/research.py`
- `backend/app/services/backtests.py`
- `backend/app/services/model_backtest_gate.py`（新增）

### 后端任务

- `backend/app/workers/backtest.py`
- `backend/app/workers/research.py`

### 核心回测

- `quant_core/model_portfolio.py`

### 前端

- `frontend/src/views/BacktestCreate.vue`
- `frontend/src/views/BacktestDetail.vue`
- `frontend/src/views/ModelDetail.vue`
- `frontend/src/views/Records.vue`

### 测试

- `backend/tests/test_backtest_contracts.py`
- `backend/tests/test_research_contracts.py`
- `backend/tests/test_model_backtest_gate.py`（新增）
- `tests/test_model_portfolio.py`

## 六、当前生产状态

- 线上地址：`https://8.134.107.95/`
- HTTPS：正常，客户端可信连接返回 200。
- API：健康。
- Worker：正常监听任务队列。
- 前端：已部署包含调参区/封存区区分的新构建。
- 本地 Git 工作区：干净。
- 服务器 Git 工作区：干净。
- 服务器当前代码提交：`1d3a801`。
- 未修改、覆盖或删除任何已有生产研究数据。

生产部署使用：

- `/opt/quantforge/compose.platform.yaml`
- `/opt/quantforge/compose.production.yaml`

由于服务器访问 Docker Hub 可能超时，本次仍采用已验证的热更新方式：将代码复制进现有容器、提交为版本镜像、更新 `latest` 标签，再使用 Compose 强制重建对应服务。

当前版本镜像标签包括：

- `quant-platform-api:1d3a801`（与 `b39df11` API 内容相同）
- `quant-platform-worker:1d3a801`
- `quant-platform-frontend:1d3a801`（与 `b39df11` 前端内容相同）

服务器为完成前端构建安装了 Node.js 18 和 npm。构建产生的 `package-lock.json`、TypeScript 临时 `.js` 和 `.tsbuildinfo` 文件已经清理，未提交进仓库。

## 七、验证结果

已完成：

- 本地新增契约与治理测试：22 项通过。
- 核心零入选组合测试：7 项通过。
- 前端单元测试：5/5 通过。
- 前端 TypeScript 与 Vite 生产构建通过。
- 服务器完整机器学习环境中可运行的后端测试：80 项通过。
- 生产 HTTPS、API 健康检查、Worker 启动和前端新标签验证通过。

生产只读验证使用：

- 模型 ID：`1bfbada4-bea5-4593-8a09-2df6db00c2e6`
- 标准行情版本 ID：`9484ed20-20e7-4ee1-a11e-e0b783c51dc2`
- 封存评估 ID：`cfc84c8c-4d12-4670-9bc2-6385fd04582d`
- 封存日期：2024-08-07 至 2025-12-03
- 覆盖：321 个交易日
- 结果：0 笔交易、全现金、`has_eligible_selections=false`

该验证直接调用回测计算函数，没有创建回测记录，也没有修改生产数据。

## 八、未完成事项与已知限制

1. 生产 API/Worker 镜像没有包含 `httpx`，因此 `backend/tests/test_app.py` 无法在生产镜像中启动；不是测试断言失败。若要在镜像内运行完整接口测试，应增加独立测试依赖或 CI 测试镜像，不建议把无关测试库直接塞进最小生产镜像。
2. 现有 user2 封存模型属于旧记录，没有封存前组合规则，因此只能以 `legacy_default_not_preregistered` 方式生成报告，不能追溯性地伪造成已预登记。
3. 尚未通过网页真正提交一条新的最终封存回测记录，因为那会新增生产记录；本次只进行了无写入的计算验证。
4. 当前封存协议的详细交易成本使用系统默认值，模型详情页主要展示 Top-N、概率阈值、调仓频率和初始资金。后续可以增加“展开高级成交规则”界面，但服务端已经完整锁定所有字段。
5. 目前批量预测统一不可作为历史回测输入。将来若需要真正的滚动在线回测，应另建逐日保存、不可回填的预测流水，不能直接复用普通批量重算文件。

## 九、建议下一步

1. 使用新研究数据集训练一个新候选模型。
2. 在调参区完整区间内比较模型及组合规则。
3. 确认唯一候选模型后，在模型详情页预登记完整组合规则并开启最终封存区。
4. 开启后创建“最终封存区回测”，确认页面、任务、报告和审计日志完整联动。
5. 不要根据封存结果再次修改参数；如需继续优化，创建下一代研究项目并等待新的未见行情。
6. 后续可新增自动化接口测试，覆盖：
   - 客户端提交任意日期时服务端强制覆盖为完整 OOS 日期。
   - 封存回测强制覆盖为预登记参数。
   - 未完成封存评估时拒绝 `sealed_oos` 回测。
   - 调参预测产物不能用于封存回测。
   - 零入选组合正常生成全现金报告。

## 十、接手注意事项

- 不要重新搭建项目。
- 不要使用 `git reset --hard` 或覆盖用户工作区。
- 不要未经确认修改、补写或删除生产研究数据。
- 部署前先检查 Worker 是否正在执行任务，避免中断训练或数据同步。
- 服务器 Docker Hub 访问可能超时，优先复用现有镜像并采用已验证的版本提交方式。
- 修改完成后必须执行测试、提交、推送、部署和 HTTPS/健康检查。
- 最终封存区一旦开启，不得为了改善表现而调整模型或组合参数。
