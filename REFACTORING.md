# 回测可信度修正与核心解耦说明

## 改造目标

本次改造保持 Streamlit 页面和旧的 Python 导入路径可用，同时建立不依赖界面
框架的 `quant_core`。未来 FastAPI、训练 Worker 和命令行任务应直接导入该包。

## 模块边界

- `quant_core/backtesting.py`：成交模拟、资金账本和绩效指标。
- `quant_core/data.py`：Baostock 查询、数据校验和重试。
- `quant_core/strategy_runtime.py`：策略配置解析；自定义代码默认禁止执行。
- `backtest.py`：旧导入路径兼容层，不再保存实现。
- `data_loader.py`：Streamlit 缓存和错误展示适配层。
- `app.py`：只读取页面状态，再将普通参数传给核心函数。

## 回测口径变化

1. 收盘信号改为下一交易日开盘成交，消除同一根K线信号与成交价混用。
2. 买入数量按照100股整手向下取整。
3. 买入当日禁止卖出，下一交易日起允许止损或止盈。
4. 佣金支持最低收费，卖出端单独计算印花税。
5. 止损和止盈使用 OHLC 触价模拟；同日同时触发时优先止损，采用保守结果。
6. 趋势退出和时间退出在收盘确认，下一交易日开盘成交。
7. 期末平仓的滑点、佣金和印花税进入最后净值。
8. 总收益以初始资金为基准，而不是以第一天收盘净值为基准。
9. 单笔盈亏使用买卖两端净现金流计算，而不是只比较成交价格。

## 仍然存在的模型边界

当前是日线级研究回测，尚未模拟涨跌停封单、停牌、成交量冲击、部分成交、
分红现金流和逐笔撮合。进入模拟盘或实盘前，应先接入包含交易状态和未复权/
复权因子的可靠数据源，再增加成交可行性模型。

自定义 Python 策略在当前本地 Streamlit 中仍以“受信任代码”运行。公开部署时
不得在 API 或 Worker 主进程中执行，必须迁移到无密钥、无网络、限制 CPU、内存
和运行时间的一次性容器。

## 验证方式

```powershell
python -m unittest discover -s tests -v
```

测试覆盖信号延迟成交、整手、T+1、期末费用、收益基准、输入校验和核心包边界。
